{
  "filename": "Python数据分析_数据清洗与预处理.json",
  "title": "数据清洗与预处理",
  "description": "数据清洗与预处理是数据分析师处理脏数据、缺失数据、异常数据的关键技能",
  "category": "数据分析师/Python数据分析",
  "tags": [
    "数据分析师",
    "Python数据分析",
    "数据清洗",
    "数据预处理",
    "缺失值处理",
    "异常值处理",
    "数据分析"
  ],
  "wordCount": 12373,
  "content": "\n\n# 数据清洗与预处理\n\n## 定义\n\n数据清洗与预处理是**处理脏数据、缺失数据、异常数据**的过程，是数据分析师**保证数据质量**的关键技能。\n\n对于数据分析师而言，数据清洗与预处理是**数据分析的前提**，大多数真实数据集都存在缺失值、异常值、重复值等问题。\n\n---\n\n## 核心概念\n\n### 1. 数据质量问题类型\n\n| 问题类型 | 说明 | 示例 |\n|----------|------|------|\n| **缺失值** | 数据为空 | 用户年龄缺失 |\n| **异常值** | 数据明显偏离正常范围 | 用户年龄=200岁 |\n| **重复值** | 数据重复出现 | 同一用户出现多次 |\n| **数据类型错误** | 数据类型不正确 | 年龄存储为字符串 |\n| **数据格式不一致** | 数据格式不统一 | 日期格式有YYYY-MM-DD和MM/DD/YYYY两种 |\n\n### 2. 数据清洗步骤\n\n| 步骤 | 说明 |\n|------|------|\n| **1. 数据审查** | 查看数据基本信息和统计信息 |\n| **2. 缺失值处理** | 删除或填充缺失值 |\n| **3. 异常值处理** | 识别和处理异常值 |\n| **4. 重复值处理** | 删除重复值 |\n| **5. 数据类型转换** | 转换数据类型 |\n| **6. 数据格式统一** | 统一数据格式 |\n\n### 3. 缺失值处理方法\n\n| 方法 | 说明 | 适用场景 |\n|------|------|----------|\n| **删除** | 删除缺失值所在行或列 | 缺失值比例很小 |\n| **填充（均值）** | 用均值填充缺失值 | 数值型数据，分布近似正态 |\n| **填充（中位数）** | 用中位数填充缺失值 | 数值型数据，分布有偏态 |\n| **填充（众数）** | 用众数填充缺失值 | 分类数据 |\n| **填充（前值/后值）** | 用前一个或后一个值填充 | 时间序列数据 |\n| **插值** | 用插值方法填充缺失值 | 连续型数据 |\n\n---\n\n## 详细内容\n\n### 一、数据审查\n\n#### 1.1 查看数据基本信息\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 查看前几行\nprint(df.head())\n\n# 查看数据形状\nprint(df.shape)\n\n# 查看列名\nprint(df.columns)\n\n# 查看数据类型\nprint(df.dtypes)\n\n# 查看基本统计信息\nprint(df.describe())\n\n# 查看缺失值情况\nprint(df.isnull().sum())\n```\n\n#### 1.2 查看数据分布情况\n\n```python\nimport pandas as pd\nimport matplotlib.pyplot as plt\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 绘制直方图\nplt.hist(df['age'], bins=10)\nplt.xlabel('Age')\nplt.ylabel('Frequency')\nplt.title('Age Distribution')\nplt.show()\n\n# 绘制箱线图\nplt.boxplot(df['age'])\nplt.ylabel('Age')\nplt.title('Age Boxplot')\nplt.show()\n```\n\n---\n\n### 二、缺失值处理\n\n#### 2.1 删除缺失值\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 删除包含缺失值的行\ndf_dropped_rows = df.dropna()\n\n# 删除包含缺失值的列\ndf_dropped_cols = df.dropna(axis=1)\n\n# 删除缺失值比例超过阈值的列\nthreshold = 0.5  # 缺失值比例超过50%的列\ndf_dropped_threshold = df.dropna(axis=1, thresh=len(df) * threshold)\n```\n\n#### 2.2 填充缺失值\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 填充为0\ndf_filled_zero = df.fillna(0)\n\n# 填充为均值\ndf_filled_mean = df.fillna(df.mean())\n\n# 填充为中位数\ndf_filled_median = df.fillna(df.median())\n\n# 填充为众数\ndf_filled_mode = df.fillna(df.mode().iloc[0])\n\n# 填充为前一个值（前向填充）\ndf_filled_ffill = df.fillna(method='ffill')\n\n# 填充为后一个值（后向填充）\ndf_filled_bfill = df.fillna(method='bfill')\n\n# 插值填充\ndf_interpolated = df.interpolate()\n```\n\n---\n\n### 三、异常值处理\n\n#### 3.1 识别异常值（Z-Score方法）\n\n```python\nimport pandas as pd\nimport numpy as np\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 计算Z-Score\nz_scores = np.abs((df['age'] - df['age'].mean()) / df['age'].std())\n\n# 识别异常值（Z-Score > 3）\noutliers = z_scores > 3\nprint(f'异常值数量: {outliers.sum()}')\n\n# 查看异常值\nprint(df[outliers])\n```\n\n#### 3.2 识别异常值（IQR方法）\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 计算IQR\nQ1 = df['age'].quantile(0.25)\nQ3 = df['age'].quantile(0.75)\nIQR = Q3 - Q1\n\n# 识别异常值（小于Q1-1.5*IQR或大于Q3+1.5*IQR）\nlower_bound = Q1 - 1.5 * IQR\nupper_bound = Q3 + 1.5 * IQR\noutliers = (df['age'] < lower_bound) | (df['age'] > upper_bound)\nprint(f'异常值数量: {outliers.sum()}')\n\n# 查看异常值\nprint(df[outliers])\n```\n\n#### 3.3 处理异常值\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 计算IQR\nQ1 = df['age'].quantile(0.25)\nQ3 = df['age'].quantile(0.75)\nIQR = Q3 - Q1\n\n# 识别异常值\nlower_bound = Q1 - 1.5 * IQR\nupper_bound = Q3 + 1.5 * IQR\noutliers = (df['age'] < lower_bound) | (df['age'] > upper_bound)\n\n# 方法1：删除异常值\ndf_no_outliers = df[~outliers]\n\n# 方法2：替换异常值为上下限\ndf['age'] = df['age'].clip(lower=lower_bound, upper=upper_bound)\n```\n\n---\n\n### 四、重复值处理\n\n#### 4.1 识别重复值\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 识别重复行\nduplicates = df.duplicated()\nprint(f'重复行数量: {duplicates.sum()}')\n\n# 查看重复行\nprint(df[duplicates])\n```\n\n#### 4.2 删除重复值\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 删除重复行（保留第一个）\ndf_deduplicated = df.drop_duplicates()\n\n# 删除重复行（保留最后一个）\ndf_deduplicated_last = df.drop_duplicates(keep='last')\n\n# 基于特定列删除重复行\ndf_deduplicated_subset = df.drop_duplicates(subset=['user_id', 'product_id'])\n```\n\n---\n\n### 五、数据类型转换\n\n#### 5.1 转换数据类型\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 查看数据类型\nprint(df.dtypes)\n\n# 转换数据类型\ndf['age'] = df['age'].astype(int)\ndf['salary'] = df['salary'].astype(float)\ndf['is_active'] = df['is_active'].astype(bool)\n```\n\n#### 5.3 转换日期类型\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 转换日期类型\ndf['order_date'] = pd.to_datetime(df['order_date'])\n\n# 提取日期部分\ndf['order_year'] = df['order_date'].dt.year\ndf['order_month'] = df['order_date'].dt.month\ndf['order_day'] = df['order_date'].dt.day\n```\n\n---\n\n### 六、数据格式统一\n\n#### 6.1 统一字符串格式\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 转换为小写\ndf['category'] = df['category'].str.lower()\n\n# 去除空格\ndf['category'] = df['category'].str.strip()\n\n# 替换字符串\ndf['category'] = df['category'].str.replace(' ', '_')\n```\n\n#### 6.2 统一日期格式\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 转换日期格式\ndf['order_date'] = pd.to_datetime(df['order_date']).dt.strftime('%Y-%m-%d')\n```\n\n---\n\n## 示例/应用场景\n\n### 示例1：电商用户数据清洗\n\n**业务问题**：清洗电商用户数据（处理缺失值、异常值、重复值等）。\n\n**分析**：\n\n```python\nimport pandas as pd\nimport numpy as np\n\n# 读取数据\ndf = pd.read_csv('users.csv')\n\n# 1. 数据审查\nprint(df.info())\nprint(df.describe())\nprint(df.isnull().sum())\n\n# 2. 缺失值处理（填充年龄的缺失值为均值）\ndf['age'] = df['age'].fillna(df['age'].mean())\n\n# 3. 异常值处理（用IQR方法识别和处理年龄异常值）\nQ1 = df['age'].quantile(0.25)\nQ3 = df['age'].quantile(0.75)\nIQR = Q3 - Q1\nlower_bound = Q1 - 1.5 * IQR\nupper_bound = Q3 + 1.5 * IQR\ndf['age'] = df['age'].clip(lower=lower_bound, upper=upper_bound)\n\n# 4. 重复值处理（删除重复的用户）\ndf = df.drop_duplicates(subset=['user_id'])\n\n# 5. 数据类型转换（转换注册时间为日期类型）\ndf['register_time'] = pd.to_datetime(df['register_time'])\n\n# 6. 数据格式统一（统一性别格式为小写）\ndf['gender'] = df['gender'].str.lower()\n\n# 保存清洗后的数据\ndf.to_csv('users_cleaned.csv', index=False)\n```\n\n**洞察与建议**：\n- 清洗后的数据质量更高，分析结果更可靠\n- 可以基于清洗后的数据进行分析和建模\n\n### 示例2：电商销售数据清洗\n\n**业务问题**：清洗电商销售数据（处理缺失值、异常值、重复值等）。\n\n**分析**：\n\n```python\nimport pandas as pd\nimport numpy as np\n\n# 读取数据\ndf = pd.read_csv('sales.csv')\n\n# 1. 数据审查\nprint(df.info())\nprint(df.describe())\nprint(df.isnull().sum())\n\n# 2. 缺失值处理（删除金额为空的行）\ndf = df.dropna(subset=['amount'])\n\n# 3. 异常值处理（用Z-Score方法识别和处理金额异常值）\nz_scores = np.abs((df['amount'] - df['amount'].mean()) / df['amount'].std())\noutliers = z_scores > 3\ndf = df[~outliers]\n\n# 4. 重复值处理（删除重复的订单）\ndf = df.drop_duplicates(subset=['order_id'])\n\n# 5. 数据类型转换（转换下单时间为日期类型）\ndf['order_time'] = pd.to_datetime(df['order_time'])\n\n# 6. 数据格式统一（统一商品类别格式为小写）\ndf['category'] = df['category'].str.lower()\n\n# 保存清洗后的数据\ndf.to_csv('sales_cleaned.csv', index=False)\n```\n\n**洞察与建议**：\n- 清洗后的数据质量更高，分析结果更可靠\n- 可以基于清洗后的数据进行分析和建模\n\n### 示例3：电商商品数据清洗\n\n**业务问题**：清洗电商商品数据（处理缺失值、异常值、重复值等）。\n\n**分析**：\n\n```python\nimport pandas as pd\nimport numpy as np\n\n# 读取数据\ndf = pd.read_csv('products.csv')\n\n# 1. 数据审查\nprint(df.info())\nprint(df.describe())\nprint(df.isnull().sum())\n\n# 2. 缺失值处理（填充库存的缺失值为0）\ndf['stock'] = df['stock'].fillna(0)\n\n# 3. 异常值处理（用IQR方法识别和处理价格异常值）\nQ1 = df['price'].quantile(0.25)\nQ3 = df['price'].quantile(0.75)\nIQR = Q3 - Q1\nlower_bound = Q1 - 1.5 * IQR\nupper_bound = Q3 + 1.5 * IQR\ndf['price'] = df['price'].clip(lower=lower_bound, upper=upper_bound)\n\n# 4. 重复值处理（删除重复的商品）\ndf = df.drop_duplicates(subset=['product_id'])\n\n# 5. 数据类型转换（转换上架时间为日期类型）\ndf['launch_date'] = pd.to_datetime(df['launch_date'])\n\n# 6. 数据格式统一（统一商品名称格式为标题格式）\ndf['product_name'] = df['product_name'].str.title()\n\n# 保存清洗后的数据\ndf.to_csv('products_cleaned.csv', index=False)\n```\n\n**洞察与建议**：\n- 清洗后的数据质量更高，分析结果更可靠\n- 可以基于清洗后的数据进行分析和建模\n\n---\n\n## 数据分析师考点\n\n### 数据清洗与预处理常见考点\n\n1. **数据质量问题类型**：缺失值、异常值、重复值、数据类型错误、数据格式不一致\n2. **数据清洗步骤**：数据审查、缺失值处理、异常值处理、重复值处理、数据类型转换、数据格式统一\n3. **缺失值处理方法**：删除、填充（均值、中位数、众数、前值/后值）、插值\n4. **异常值识别方法**：Z-Score方法、IQR方法\n5. **异常值处理方法**：删除、替换\n6. **重复值处理方法**：删除\n7. **数据类型转换方法**：`astype()`、`pd.to_datetime()`\n8. **数据格式统一方法**：字符串格式统一、日期格式统一\n\n### 实战考点\n\n1. **电商用户数据清洗**：\n   - 如何处理用户数据中的缺失值\n   - 如何处理用户数据中的异常值\n   - 如何处理用户数据中的重复值\n   - 如何转换用户数据中的数据类型\n   - 如何统一用户数据中的数据格式\n2. **电商销售数据清洗**：\n   - 如何处理销售数据中的缺失值\n   - 如何处理销售数据中的异常值\n   - 如何处理销售数据中的重复值\n   - 如何转换销售数据中的数据类型\n   - 如何统一销售数据中的数据格式\n3. **电商商品数据清洗**：\n   - 如何处理商品数据中的缺失值\n   - 如何处理商品数据中的异常值\n   - 如何处理商品数据中的重复值\n   - 如何转换商品数据中的数据类型\n   - 如何统一商品数据中的数据格式\n\n---\n\n## 最佳实践\n\n### 1. 先进行数据审查，再进行处理\n\n**不推荐**（不审查数据，直接处理）：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 直接填充缺失值\ndf = df.fillna(0)\n```\n\n**推荐**（先审查数据，再进行处理）：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 数据审查\nprint(df.info())\nprint(df.describe())\nprint(df.isnull().sum())\n\n# 根据审查结果，选择合适的处理方法\ndf = df.fillna(df.mean())\n```\n\n### 2. 根据数据特征，选择合适的缺失值处理方法\n\n**不推荐**（盲目使用一种缺失值处理方法）：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 盲目使用填充均值的方\ndf = df.fillna(df.mean())\n```\n\n**推荐**（根据数据特征，选择合适的缺失值处理方法）：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 对于数值型数据，分布近似正态，使用填充均值\ndf['age'] = df['age'].fillna(df['age'].mean())\n\n# 对于数值型数据，分布有偏态，使用填充中位数\ndf['salary'] = df['salary'].fillna(df['salary'].median())\n\n# 对于分类数据，使用填充众数\ndf['category'] = df['category'].fillna(df['category'].mode()[0])\n\n# 对于时间序列数据，使用填充前一个值\ndf['temperature'] = df['temperature'].fillna(method='ffill')\n```\n\n### 3. 根据业务需求，选择合适的异常值处理方法\n\n**不推荐**（盲目使用一种异常值处理方法）：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 盲目删除异常值\ndf = df[~((df['age'] - df['age'].mean()) / df['age'].std()).abs() > 3]\n```\n\n**推荐**（根据业务需求，选择合适的异常值处理方法）：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 如果异常值是由于数据录入错误导致的，可以删除或替换\ndf['age'] = df['age'].clip(lower=0, upper=100)\n\n# 如果异常值是由于业务特殊性导致的，可以保留\n# 例如：奢侈品销售数据中的高额订单\n```\n\n### 4. 记录数据清洗过程，保证可复现性\n\n**不推荐**（不记录数据清洗过程）：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 填充缺失值\ndf = df.fillna(0)\n\n# 删除异常值\ndf = df[~((df['age'] - df['age'].mean()) / df['age'].std()).abs() > 3]\n\n# 保存清洗后的数据\ndf.to_csv('data_cleaned.csv', index=False)\n```\n\n**推荐**（记录数据清洗过程）：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 记录数据清洗过程\nwith open('data_cleaning_log.txt', 'w') as f:\n    f.write('数据清洗过程记录\\n')\n    f.write('===================\\n\\n')\n    f.write('1. 数据审查\\n')\n    f.write(f'原始数据形状: {df.shape}\\n')\n    f.write(f'缺失值情况:\\n{df.isnull().sum()}\\n\\n')\n    \n    # 填充缺失值\n    df = df.fillna(0)\n    f.write('2. 缺失值处理\\n')\n    f.write('填充缺失值为0\\n\\n')\n    \n    # 删除异常值\n    df = df[~((df['age'] - df['age'].mean()) / df['age'].std()).abs() > 3]\n    f.write('3. 异常值处理\\n')\n    f.write('删除Z-Score > 3的异常值\\n\\n')\n    \n    # 保存清洗后的数据\n    df.to_csv('data_cleaned.csv', index=False)\n    f.write('4. 保存清洗后的数据\\n')\n    f.write('清洗后的数据保存为data_cleaned.csv\\n')\n```\n\n---\n\n## 常见错误\n\n### 1. 不审查数据，直接处理\n\n**错误示例**：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 直接填充缺失值\ndf = df.fillna(0)\n```\n\n**正确做法**：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 数据审查\nprint(df.info())\nprint(df.describe())\nprint(df.isnull().sum())\n\n# 根据审查结果，选择合适的处理方法\ndf = df.fillna(df.mean())\n```\n\n### 2. 盲目使用一种缺失值处理方法\n\n**错误示例**：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 盲目使用填充均值的方\ndf = df.fillna(df.mean())\n```\n\n**正确做法**：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 对于数值型数据，分布近似正态，使用填充均值\ndf['age'] = df['age'].fillna(df['age'].mean())\n\n# 对于数值型数据，分布有偏态，使用填充中位数\ndf['salary'] = df['salary'].fillna(df['salary'].median())\n\n# 对于分类数据，使用填充众数\ndf['category'] = df['category'].fillna(df['category'].mode()[0])\n\n# 对于时间序列数据，使用填充前一个值\ndf['temperature'] = df['temperature'].fillna(method='ffill')\n```\n\n### 3. 盲目使用一种异常值处理方法\n\n**错误示例**：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 盲目删除异常值\ndf = df[~((df['age'] - df['age'].mean()) / df['age'].std()).abs() > 3]\n```\n\n**正确做法**：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 如果异常值是由于数据录入错误导致的，可以删除或替换\ndf['age'] = df['age'].clip(lower=0, upper=100)\n\n# 如果异常值是由于业务特殊性导致的，可以保留\n# 例如：奢侈品销售数据中的高额订单\n```\n\n### 4. 不记录数据清洗过程，导致不可复现\n\n**错误示例**：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 填充缺失值\ndf = df.fillna(0)\n\n# 删除异常值\ndf = df[~((df['age'] - df['age'].mean()) / df['age'].std()).abs() > 3]\n\n# 保存清洗后的数据\ndf.to_csv('data_cleaned.csv', index=False)\n```\n\n**正确做法**：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 记录数据清洗过程\nwith open('data_cleaning_log.txt', 'w') as f:\n    f.write('数据清洗过程记录\\n')\n    f.write('===================\\n\\n')\n    f.write('1. 数据审查\\n')\n    f.write(f'原始数据形状: {df.shape}\\n')\n    f.write(f'缺失值情况:\\n{df.isnull().sum()}\\n\\n')\n    \n    # 填充缺失值\n    df = df.fillna(0)\n    f.write('2. 缺失值处理\\n')\n    f.write('填充缺失值为0\\n\\n')\n    \n    # 删除异常值\n    df = df[~((df['age'] - df['age'].mean()) / df['age'].std()).abs() > 3]\n    f.write('3. 异常值处理\\n')\n    f.write('删除Z-Score > 3的异常值\\n\\n')\n    \n    # 保存清洗后的数据\n    df.to_csv('data_cleaned.csv', index=False)\n    f.write('4. 保存清洗后的数据\\n')\n    f.write('清洗后的数据保存为data_cleaned.csv\\n')\n```\n\n### 5. 不验证清洗后的数据质量\n\n**错误示例**：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 填充缺失值\ndf = df.fillna(0)\n\n# 删除异常值\ndf = df[~((df['age'] - df['age'].mean()) / df['age'].std()).abs() > 3]\n\n# 保存清洗后的数据\ndf.to_csv('data_cleaned.csv', index=False)\n```\n\n**正确做法**：\n\n```python\nimport pandas as pd\n\n# 读取数据\ndf = pd.read_csv('data.csv')\n\n# 填充缺失值\ndf = df.fillna(0)\n\n# 删除异常值\ndf = df[~((df['age'] - df['age'].mean()) / df['age'].std()).abs() > 3]\n\n# 验证清洗后的数据质量\nprint('清洗后的数据质量验证')\nprint('==========================')\nprint(f'数据形状: {df.shape}')\nprint(f'缺失值情况:\\n{df.isnull().sum()}')\nprint(f'基本统计信息:\\n{df.describe()}')\n\n# 保存清洗后的数据\ndf.to_csv('data_cleaned.csv', index=False)\n```\n\n---\n\n## 总结\n\n数据清洗与预处理是数据分析师的关键技能，关键要点包括：\n\n1. **掌握数据质量问题类型**：缺失值、异常值、重复值、数据类型错误、数据格式不一致\n2. **掌握数据清洗步骤**：数据审查、缺失值处理、异常值处理、重复值处理、数据类型转换、数据格式统一\n3. **熟练使用缺失值处理方法**：删除、填充（均值、中位数、众数、前值/后值）、插值\n4. **熟练使用异常值识别方法**：Z-Score方法、IQR方法\n5. **熟练使用异常值处理方法**：删除、替换\n6. **熟练使用重复值处理方法**：删除\n7. **熟练使用数据类型转换方法**：`astype()`、`pd.to_datetime()`\n8. **熟练使用数据格式统一方法**：字符串格式统一、日期格式统一\n9. **注意最佳实践**：先进行数据审查再进行处理、根据数据特征选择合适的缺失值处理方法、根据业务需求选择合适的异常值处理方法、记录数据清洗过程保证可复现性\n10. **避免常见错误**：不审查数据直接处理、盲目使用一种缺失值处理方法、盲目使用一种异常值处理方法、不记录数据清洗过程导致不可复现、不验证清洗后的数据质量\n\n数据清洗与预处理是数据分析师的必备技能，需要在实践中不断积累经验。\n\n---\n\n## 扩展阅读\n\n### 高级主题\n\n1. **高级缺失值处理方法**：多重插补（Multiple Imputation）、KNN插补\n2. **高级异常值检测方法**：孤立森林（Isolation Forest）、LOF（Local Outlier Factor）\n3. **高级数据清洗工具**：OpenRefine、Great Expectations\n4. **数据清洗自动化**：使用Pipeline自动化数据清洗过程\n\n### 实战案例\n\n1. **电商用户数据清洗**：处理用户数据中的缺失值、异常值、重复值等\n2. **电商销售数据清洗**：处理销售数据中的缺失值、异常值、重复值等\n3. **电商商品数据清洗**：处理商品数据中的缺失值、异常值、重复值等\n4. **金融数据清洗**：处理金融数据中的缺失值、异常值、重复值等\n\n---\n\n**注**：本文件内容适用于所有需要进行数据清洗与预处理的场景，是数据分析师的必备基础知识。\n\n---\n"
}