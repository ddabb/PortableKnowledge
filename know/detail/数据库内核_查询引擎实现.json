{
  "filename": "数据库内核_查询引擎实现.json",
  "title": "查询引擎实现",
  "description": "查询引擎的核心实现技术，涵盖查询解析、查询优化（RBO/CBO）、查询执行（火山模型/向量化/编译执行）和代价模型",
  "category": "技术/数据库内核",
  "tags": [
    "查询引擎",
    "查询解析",
    "查询优化",
    "RBO",
    "CBO",
    "查询执行",
    "火山模型",
    "向量化执行",
    "编译执行",
    "代价模型"
  ],
  "wordCount": 8591,
  "content": "\n\n# 查询引擎实现\n\n## 定义\n\n**查询引擎**是数据库内核中负责处理SQL查询的核心组件，包括查询解析、查询优化和查询执行三个主要阶段。\n\n**核心目标**：\n- **正确解析**：将SQL文本转换为逻辑查询计划\n- **优化查询**：生成最优物理查询计划\n- **高效执行**：快速返回查询结果\n\n---\n\n## 核心概念\n\n### 1. 查询处理流程\n\n| 步骤 | 说明 | 输出 |\n|------|------|------|\n| **解析（Parsing）** | 语法分析，生成解析树 | 解析树（Parse Tree） |\n| **重写（Rewriting）** | 语义分析、视图展开、规则重写 | 逻辑查询计划（Logical Plan） |\n| **优化（Optimization）** | 生成最优执行计划（基于统计信息） | 物理查询计划（Physical Plan） |\n| **执行（Execution）** | 按执行计划执行，返回结果 | 查询结果 |\n\n### 2. 查询优化分类\n\n| 优化类型 | 说明 | 示例 |\n|----------|------|------|\n| **基于规则优化（RBO）** | 使用预定义规则重写查询 | 谓词下推、投影下推、子查询展开 |\n| **基于代价优化（CBO）** | 估计不同执行计划的代价，选择代价最小的 | 选择连接顺序、选择索引、选择连接算法 |\n\n### 3. 查询执行模型\n\n| 执行模型 | 说明 | 优点 | 缺点 |\n|----------|------|------|------|\n| **火山模型（Volcano Model）** | 拉取式（Pull-based），每个操作符实现`next()`方法 | 简单，易实现 | 函数调用开销大 |\n| **向量化执行（Vectorized Execution）** | 一次处理一批数据（如1024行） | 减少函数调用开销，提升缓存局部性 | 实现复杂 |\n| **编译执行（Compiled Execution）** | 将查询计划编译为机器码 | 减少解释执行开销 | 编译开销大 |\n\n---\n\n## 详细内容\n\n### 一、查询解析（Query Parsing）\n\n#### 1.1 词法分析（Lexical Analysis）\n\n**目标**：将SQL文本拆分为词法单元（Token）。\n\n**示例**：\n```sql\nSELECT * FROM users WHERE age > 18\n```\n\n**Token序列**：\n```\nSELECT, *, FROM, users, WHERE, age, >, 18\n```\n\n**实现工具**：\n- **Flex**：生成词法分析器\n- **手写的词法分析器**：如PostgreSQL的`scan.l`\n\n#### 1.2 语法分析（Syntax Analysis）\n\n**目标**：根据语法规则，将Token序列转换为解析树。\n\n**语法规则**（简化）：\n```\nSelectStmt:\n    SELECT target_list FROM from_clause WHERE where_clause\n```\n\n**解析树示例**（抽象语法树AST）：\n```\nSelectStmt\n├── target_list: *\n├── from_clause: users\n└── where_clause: age > 18\n```\n\n**实现工具**：\n- **Bison**：生成语法分析器\n- **手写的语法分析器**：如PostgreSQL的`gram.y`\n\n#### 1.3 语义分析（Semantic Analysis）\n\n**目标**：检查解析树的语义正确性。\n\n**检查内容**：\n- 表是否存在\n- 列是否存在\n- 数据类型是否匹配\n- 权限检查\n\n**输出**：**查询树（Query Tree）**，包含详细的语义信息。\n\n### 二、查询重写（Query Rewriting）\n\n#### 2.1 视图展开（View Expension）\n\n**示例**：\n```sql\nCREATE VIEW young_users AS SELECT * FROM users WHERE age <= 35;\n\nSELECT * FROM young_users WHERE city = 'Beijing';\n```\n\n**重写后**：\n```sql\nSELECT * FROM users WHERE age <= 35 AND city = 'Beijing';\n```\n\n#### 2.2 谓词下推（Predicate Pushdown）\n\n**原理**：将过滤条件尽可能下推到数据源附近，减少后续处理的数据量。\n\n**示例**：\n```sql\nSELECT * FROM (SELECT * FROM users WHERE age > 18) AS t WHERE city = 'Beijing';\n```\n\n**重写后**：\n```sql\nSELECT * FROM users WHERE age > 18 AND city = 'Beijing';\n```\n\n#### 2.3 投影下推（Projection Pushdown）\n\n**原理**：将投影（列选择）尽可能下推到数据源附近，减少后续处理的数据量。\n\n**示例**：\n```sql\nSELECT name FROM (SELECT * FROM users WHERE age > 18) AS t;\n```\n\n**重写后**：\n```sql\nSELECT name FROM users WHERE age > 18;\n```\n\n#### 2.4 子查询展开（Subquery Unnesting）\n\n**示例**：\n```sql\nSELECT * FROM users WHERE id IN (SELECT user_id FROM orders WHERE amount > 100);\n```\n\n**重写后**（转换为连接）：\n```sql\nSELECT users.* FROM users JOIN orders ON users.id = orders.user_id WHERE orders.amount > 100;\n```\n\n### 三、查询优化（Query Optimization）\n\n#### 3.1 基于规则优化（RBO）\n\n**原理**：使用预定义规则重写查询，规则基于启发式经验。\n\n**常见规则**：\n1. **谓词下推**（Predicate Pushdown）\n2. **投影下推**（Projection Pushdown）\n3. **子查询展开**（Subquery Unnesting）\n4. **外连接消除**（Outer Join Elimination）\n5. **冗余子表达式消除**（Common Subexpression Elimination）\n\n**优势**：\n- 简单快速\n- 总是正确（规则基于数学等价变换）\n\n**劣势**：\n- 不考虑数据分布和代价\n- 可能不是最优执行计划\n\n#### 3.2 基于代价优化（CBO）\n\n**原理**：估计不同执行计划的代价，选择代价最小的执行计划。\n\n**代价模型**：\n```\n总代价 = I/O代价 + CPU代价 + 网络代价（分布式）\n```\n\n**统计信息**：\n- **表统计信息**：行数、页数、列基数\n- **列统计信息**：最小值、最大值、直方图、最常见值\n\n**优化步骤**：\n1. **生成搜索空间**：枚举所有可能的执行计划\n2. **估计代价**：使用代价模型和统计信息估计每个执行计划的代价\n3. **选择最优计划**：选择代价最小的执行计划\n\n**挑战**：\n- 搜索空间巨大（组合爆炸）\n- 统计信息可能不准确\n- 代价模型可能不准确\n\n#### 3.3 连接顺序选择（Join Order Selection）\n\n**问题**：多表连接时，连接顺序对性能影响巨大。\n\n**动态规划算法**（适用于少量表）：\n- 枚举所有连接顺序\n- 使用代价模型选择最优\n\n**贪心算法**（适用于大量表）：\n- 每次选择代价最小的连接\n- 可能不是全局最优\n\n**示例**（3表连接）：\n```sql\nSELECT * FROM t1, t2, t3 WHERE t1.id = t2.id AND t2.id = t3.id;\n```\n\n**可能的连接顺序**：\n1. (t1 ⋈ t2) ⋈ t3\n2. (t1 ⋈ t3) ⋈ t2\n3. (t2 ⋈ t3) ⋈ t1\n...\n\n**CBO选择代价最小的连接顺序**。\n\n#### 3.4 索引选择（Index Selection）\n\n**问题**：查询可能涉及多个索引，需要选择最优索引。\n\n**考虑因素**：\n- 索引选择性（Selectivity）\n- 索引覆盖度（Covering Index）\n- 索引访问代价\n\n**示例**：\n```sql\nSELECT * FROM users WHERE age > 18 AND city = 'Beijing';\n```\n\n**可能的索引**：\n- `idx_age` (age)\n- `idx_city` (city)\n- `idx_age_city` (age, city)\n\n**CBO选择代价最小的索引**。\n\n### 四、查询执行（Query Execution）\n\n#### 4.1 火山模型（Volcano Model）\n\n**原理**：拉取式（Pull-based），每个操作符实现`next()`方法，返回下一个元组。\n\n**示例**（扫描+过滤）：\n```python\nclass ScanOperator:\n    def __init__(self, table):\n        self.table = table\n        self.cursor = 0\n    \n    def next(self):\n        if self.cursor >= len(self.table):\n            return None\n        row = self.table[self.cursor]\n        self.cursor += 1\n        return row\n\nclass FilterOperator:\n    def __init__(self, child, predicate):\n        self.child = child\n        self.predicate = predicate\n    \n    def next(self):\n        while True:\n            row = self.child.next()\n            if row is None:\n                return None\n            if self.predicate(row):\n                return row\n```\n\n**优势**：\n- 简单，易实现\n- 适合流式处理\n\n**劣势**：\n- 函数调用开销大（每个元组都要调用`next()`）\n- 缓存局部性差\n\n#### 4.2 向量化执行（Vectorized Execution）\n\n**原理**：一次处理一批数据（如1024行），减少函数调用开销，提升缓存局部性。\n\n**示例**（扫描+过滤）：\n```python\nclass VectorizedScanOperator:\n    def __init__(self, table):\n        self.table = table\n        self.cursor = 0\n    \n    def next_batch(self, batch_size):\n        if self.cursor >= len(self.table):\n            return None\n        batch = self.table[self.cursor:self.cursor + batch_size]\n        self.cursor += batch_size\n        return batch\n\nclass VectorizedFilterOperator:\n    def __init__(self, child, predicate):\n        self.child = child\n        self.predicate = predicate\n    \n    def next_batch(self, batch_size):\n        while True:\n            batch = self.child.next_batch(batch_size)\n            if batch is None:\n                return None\n            filtered = [row for row in batch if self.predicate(row)]\n            if filtered:\n                return filtered\n```\n\n**优势**：\n- 减少函数调用开销\n- 提升缓存局部性\n- 适合SIMD指令优化\n\n**劣势**：\n- 实现复杂\n- 不适合流式处理（需要攒批）\n\n#### 4.3 编译执行（Compiled Execution）\n\n**原理**：将查询计划编译为机器码，减少解释执行开销。\n\n**示例**（使用LLVM编译执行）：\n```cpp\n// 查询：SELECT * FROM t WHERE a > 10\n\n// 编译后的机器码（伪代码）：\nvoid query_execution() {\n    for (int i = 0; i < t_size; i++) {\n        if (t[i].a > 10) {\n            emit(t[i]);\n        }\n    }\n}\n```\n\n**优势**：\n- 减少解释执行开销\n- 适合复杂查询\n\n**劣势**：\n- 编译开销大\n- 实现复杂\n\n---\n\n## 示例/应用场景\n\n### 示例1：查询优化实战（PostgreSQL）\n\n**场景**：分析一个复杂查询的执行计划。\n\n**查询**：\n```sql\nEXPLAIN ANALYZE\nSELECT u.name, o.order_id\nFROM users u\nJOIN orders o ON u.id = o.user_id\nWHERE u.age > 18 AND o.amount > 100;\n```\n\n**执行计划**（简化）：\n```\nNested Loop Join (cost=10.0..100.0 rows=100 width=64)\n  -> Seq Scan on users u (cost=0.0..10.0 rows=100 width=32)\n       Filter: age > 18\n  -> Index Scan using idx_order_user_id on orders o (cost=0.0..1.0 rows=10 width=32)\n       Index Cond: user_id = u.id\n       Filter: amount > 100\n```\n\n**分析**：\n1. **扫描users表**：使用顺序扫描（Seq Scan），过滤`age > 18`\n2. **扫描orders表**：使用索引扫描（Index Scan），使用索引`idx_order_user_id`\n3. **连接**：使用嵌套循环连接（Nested Loop Join）\n\n**优化建议**：\n- 在`users.age`上创建索引，加速过滤\n- 在`orders.user_id`和`orders.amount`上创建复合索引，加速连接和过滤\n\n### 示例2：向量化执行实战（ClickHouse）\n\n**场景**：ClickHouse使用向量化执行加速查询。\n\n**查询**：\n```sql\nSELECT sum(amount) FROM orders WHERE order_date > '2023-01-01';\n```\n\n**向量化执行流程**：\n1. **扫描orders表**：一次读取1024行\n2. **过滤**：使用SIMD指令并行过滤1024行\n3. **聚合**：使用向量化聚合函数（如`sum()`）计算1024行的和\n4. **返回结果**：返回聚合结果\n\n**性能对比**：\n| 执行模型 | 查询时间 |\n|----------|----------|\n| 火山模型 | 10s |\n| 向量化执行 | 2s |\n\n---\n\n## 【对应领域考点】\n\n### 查询引擎实现常见考点\n\n1. **查询处理流程**：解析、重写、优化、执行\n2. **查询解析**：词法分析、语法分析、语义分析\n3. **查询重写**：视图展开、谓词下推、投影下推、子查询展开\n4. **查询优化**：RBO、CBO、连接顺序选择、索引选择\n5. **查询执行**：火山模型、向量化执行、编译执行\n\n### 实战考点\n\n1. **查询解析考点**：\n   - 词法分析工具（Flex）\n   - 语法分析工具（Bison）\n   - 解析树（Parse Tree）和查询树（Query Tree）\n\n2. **查询重写考点**：\n   - 视图展开\n   - 谓词下推\n   - 投影下推\n   - 子查询展开\n\n3. **查询优化考点**：\n   - RBO规则\n   - CBO代价模型\n   - 统计信息\n   - 连接顺序选择算法（动态规划、贪心）\n   - 索引选择\n\n4. **查询执行考点**：\n   - 火山模型原理和实现\n   - 向量化执行原理和实现\n   - 编译执行原理和实现\n   - 三种执行模型对比\n\n---\n\n## 最佳实践\n\n### 1. 系统化查询引擎学习路径\n\n**不推荐**（零散学习）：\n```text\n不系统化学习，随意阅读源码，导致理解不全面\n```\n\n**推荐**（系统化路径）：\n```text\n1. 掌握基础理论：\n   - 查询处理流程（解析、重写、优化、执行）\n   - 查询解析（词法分析、语法分析、语义分析）\n   - 查询重写（视图展开、谓词下推、投影下推、子查询展开）\n   - 查询优化（RBO、CBO、连接顺序选择、索引选择）\n   - 查询执行（火山模型、向量化执行、编译执行）\n2. 阅读经典论文：\n   - Volcano执行模型\n   - PostgreSQL查询优化器\n   - Column-Oriented数据库向量化执行\n3. 阅读开源数据库源码：\n   - PostgreSQL（查询解析、优化、执行）\n   - MySQL（查询优化、执行）\n   - ClickHouse（向量化执行）\n4. 实践项目：\n   - 实现简单查询解析器\n   - 实现简单查询优化器\n   - 实现简单查询执行器\n```\n\n### 2. 深入理解CBO优化器\n\n**不推荐**（只了解概念）：\n```text\n只了解CBO概念，不理解实现细节\n```\n\n**推荐**（深入理解）：\n```text\n- 理解代价模型（I/O代价、CPU代价、网络代价）\n- 掌握统计信息（表统计信息、列统计信息、直方图）\n- 理解连接顺序选择算法（动态规划、贪心）\n- 阅读PostgreSQL/MySQL优化器源码\n```\n\n### 3. 掌握向量化执行\n\n**不推荐**（只了解概念）：\n```text\n只了解向量化执行概念，不理解实现细节\n```\n\n**推荐**（掌握实现）：\n```text\n- 理解向量化执行原理（一次处理一批数据）\n- 掌握向量化执行优势（减少函数调用开销、提升缓存局部性）\n- 阅读ClickHouse/Arrow向量化执行源码\n- 实践向量化执行（使用SIMD指令）\n```\n\n### 4. 持续优化和监控\n\n**不推荐**（一次学习）：\n```text\n只学习一次，不持续监控和优化\n```\n\n**推荐**（持续监控优化）：\n```text\n- 监控查询性能（QPS、延迟、资源使用率）\n- 分析慢查询（慢查询日志、执行计划）\n- 优化查询（重写查询、调整连接顺序、选择索引）\n- 持续学习（新版本特性、新优化技术）\n```\n\n---\n\n## 【常见错误】\n\n### 1. 不理解查询执行计划\n\n**错误示例**：\n```text\n不理解查询执行计划，导致无法优化慢查询\n```\n\n**正确做法**：\n```text\n- 掌握执行计划读取（EXPLAIN命令）\n- 理解不同操作符（扫描、过滤、连接、聚合）\n- 分析执行计划代价（cost、rows、width）\n- 根据执行计划优化查询\n```\n\n### 2. 索引设计不当\n\n**错误示例**：\n```text\n索引设计不当，导致查询性能不佳\n```\n\n**正确做法**：\n```text\n- 选择合适索引类型（B+树、哈希、位图等）\n- 设计合适索引列（WHERE、JOIN、ORDER BY、GROUP BY列）\n- 避免索引失效（函数、类型转换、前导模糊查询）\n- 定期维护索引（重建、重组）\n```\n\n### 3. 查询设计不当\n\n**错误示例**：\n```text\n查询设计不当，导致性能差\n```\n\n**正确做法**：\n```text\n- 避免SELECT *（只选择需要的列）\n- 使用参数化查询（避免SQL注入、提升缓存命中率）\n- 优化连接顺序（小表驱动大表）\n- 避免子查询（转换为连接）\n```\n\n### 4. 忽略性能监控和优化\n\n**错误示例**：\n```text\n不监控查询性能，导致性能问题发现晚\n```\n\n**正确做法**：\n```text\n- 监控关键指标（QPS、延迟、资源使用率）\n- 分析慢查询（慢查询日志、执行计划）\n- 优化查询（重写查询、调整连接顺序、选择索引）\n- 定期性能调优\n```\n\n---\n\n## 总结\n\n查询引擎实现是数据库内核的核心，涉及查询解析、查询重写、查询优化和查询执行等多个方面。要做好查询引擎开发和优化，需要：\n\n1. **系统化学习基础理论**：查询处理流程、查询解析、查询重写、查询优化、查询执行\n2. **阅读经典论文和源码**：Volcano、PostgreSQL、ClickHouse\n3. **深入理解关键组件**：CBO优化器、向量化执行\n4. **持续监控和调优**：监控性能、分析慢查询、优化查询\n\n随着数据量越来越大、查询越来越复杂，查询引擎技术将持续发展，包括更智能的查询优化器、更高效的查询执行模型、更强大的查询优化技术。\n\n---\n\n## 扩展阅读\n\n### 高级主题\n\n1. **分布式查询引擎**：分布式查询优化、分布式查询执行\n2. **AI4Query**：人工智能优化查询（索引推荐、参数调优、查询优化）\n3. **内存查询引擎**：内存优化、持久化内存（PMEM）支持\n4. **流查询引擎**：流式查询处理、增量查询处理\n\n### 实战案例\n\n1. **PostgreSQL查询优化器源码分析**：统计信息、代价估计、执行计划生成\n2. **ClickHouse向量化执行源码分析**：向量化执行、SIMD优化\n3. **MySQL查询执行器源码分析**：火山模型、索引使用\n4. **查询性能调优实战**：索引优化、查询重写、参数调优\n\n### 工具和资源\n\n1. **数据库系统**：\n   - PostgreSQL：https://www.postgresql.org/\n   - MySQL：https://www.mysql.com/\n   - ClickHouse：https://clickhouse.com/\n\n2. **学习资源**：\n   - 经典教材：《数据库系统概念》、《数据库系统实现》\n   - 论文：Volcano、PostgreSQL查询优化器、Column-Oriented数据库向量化执行\n   - 博客：PostgreSQL官方博客、MySQL官方博客、ClickHouse官方博客\n   - 课程：Stanford CS245、CMU 15-445\n\n---\n\n**注**：本文件为查询引擎实现核心内容，适合数据库内核开发者、数据库管理员（DBA）和后端工程师学习。建议结合具体数据库系统和应用场景，实践查询引擎开发和优化，积累实战经验。"
}